---
const year = new Date().getFullYear();
const links = {
  projects: [
    { label: 'Release The Beast', href: '/release-the-beast' },
    { label: 'Rage Fighters', href: '/rage-fighters' },
    { label: 'Activations', href: '/activations' },
  ],
  resources: [
    { label: 'Engagement Guides', href: '/activations#guides' },
    { label: 'Events', href: '/events' },
    { label: 'Year in Review', href: '/year-in-review' },
    { label: 'Supporters', href: '/supporters' },
  ],
  company: [
    { label: 'About', href: '/about' },
    { label: 'Connect', href: '/connect' },
  ],
};
---

<footer class="site-footer">
  <div class="container">
    <div class="footer-top">
      <div class="footer-brand">
        <a href="/" class="footer-logo">
          <img
            src="/images/RyzoLogo_Brigher_WordsOnly.png"
            alt="Ryzo Studios"
            class="footer-logo-img"
            width="140"
            height="70"
          />
        </a>
        <p class="footer-tagline">
          Emotions aren't the enemy.<br />They're the power-up.
        </p>
        <p class="footer-desc">
          Social-emotional learning for the next generation —
          through cinema, play, and real connection.
        </p>
      </div>

      <nav class="footer-nav" aria-label="Footer navigation">
        <div class="footer-nav-group">
          <span class="footer-nav-label">Projects</span>
          <ul>
            {links.projects.map(l => (
              <li><a href={l.href}>{l.label}</a></li>
            ))}
          </ul>
        </div>
        <div class="footer-nav-group">
          <span class="footer-nav-label">Resources</span>
          <ul>
            {links.resources.map(l => (
              <li><a href={l.href}>{l.label}</a></li>
            ))}
          </ul>
        </div>
        <div class="footer-nav-group">
          <span class="footer-nav-label">Company</span>
          <ul>
            {links.company.map(l => (
              <li><a href={l.href}>{l.label}</a></li>
            ))}
          </ul>
        </div>
      </nav>
    </div>

    <div class="footer-bottom">
      <p class="footer-copy">
        © {year} Ryzo Studios. All rights reserved.
      </p>
      <p class="footer-credit">
        Backed by Stanford &amp; UCSF research. Created by former Pixar leaders.
      </p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    background: var(--bg-2);
    border-top: 1px solid var(--border);
    padding-block: var(--s-16) var(--s-8);
    margin-top: auto;
  }

  .footer-top {
    display: grid;
    grid-template-columns: 1fr 2fr;
    gap: var(--s-12);
    padding-bottom: var(--s-12);
    border-bottom: 1px solid var(--border);
  }

  .footer-brand {
    display: flex;
    flex-direction: column;
    gap: var(--s-5);
  }

  .footer-logo {
    display: inline-flex;
    width: fit-content;
  }

  .footer-logo-img {
    height: 100px;
    width: auto;
    display: block;
    object-fit: contain;
  }

  .footer-tagline {
    font-family: var(--font-display);
    font-size: var(--text-lg);
    font-weight: 600;
    line-height: 1.4;
    letter-spacing: -0.01em;
    color: var(--fg-2);
  }

  .footer-desc {
    font-size: var(--text-sm);
    color: var(--fg-2);
    line-height: 1.6;
    max-width: 300px;
  }

  .footer-nav {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--s-8);
  }

  .footer-nav-label {
    display: block;
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--fg-3);
    margin-bottom: var(--s-4);
  }

  .footer-nav-group ul {
    display: flex;
    flex-direction: column;
    gap: var(--s-2);
  }

  .footer-nav-group a {
    font-size: var(--text-sm);
    color: var(--fg-2);
    transition: color var(--transition);
  }

  .footer-nav-group a:hover {
    color: var(--fg);
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding-top: var(--s-8);
    gap: var(--s-4);
    flex-wrap: wrap;
  }

  .footer-copy,
  .footer-credit {
    font-size: var(--text-xs);
    color: var(--fg-3);
  }

  @media (max-width: 768px) {
    .footer-top {
      grid-template-columns: 1fr;
    }
    .footer-nav {
      grid-template-columns: repeat(2, 1fr);
    }
    .footer-bottom {
      flex-direction: column;
      align-items: flex-start;
    }
  }

  @media (max-width: 480px) {
    .footer-nav {
      grid-template-columns: 1fr;
    }
  }
</style>
