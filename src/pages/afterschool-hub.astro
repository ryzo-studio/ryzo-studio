---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="After-School Event Hub"
  description="Everything you need to host a cinematic, creative, and skill-building 90-minute UN-TILTED session. No advanced prep required — Aaron's story and the integrated slide deck lead the way."
>

  <!-- Hero -->
  <section class="hub-hero">
    <div class="hero-bg"></div>
    <div class="container hero-content">
      <span class="eyebrow">After-School Event Hub</span>
      <h1 class="display">
        The 90-Minute<br />
        <span class="accent-text">"UN-TILTED"</span><br />
        Experience
      </h1>
      <p class="hero-body">
        Everything you need to host a cinematic, creative, and skill-building session.
        No advanced prep or "expert" knowledge required — Aaron's story and the integrated
        slide deck lead the way.
      </p>
      <a href="#toolkit" class="btn btn-primary btn-lg">Go to the Toolkit</a>
    </div>
  </section>

  <!-- Toolkit -->
  <section class="section toolkit-section" id="toolkit">
    <div class="container">
      <div class="toolkit-header">
        <span class="eyebrow">The Toolkit</span>
        <h2 class="subheadline">Three tools.<br />One unforgettable session.</h2>
        <p class="toolkit-intro">
          Direct access to the three essential elements. Each one is designed to work
          together — open them in order and you're ready to go.
        </p>
      </div>

      <div class="toolkit-grid">

        <!-- Slide Deck -->
        <div class="toolkit-card toolkit-card--primary">
          <div class="toolkit-card-icon">⚡</div>
          <div class="toolkit-card-badge">Start Here</div>
          <h3 class="toolkit-card-title">Master Slide Deck</h3>
          <p class="toolkit-card-desc">
            This is the heart of the event. Just click <strong>'Present.'</strong> The Sticky Stan
            expression sheets, the film link, and the game mission are all built-in. Use the
            <strong>'Presenter Notes'</strong> for your script.
          </p>
          <a href="https://docs.google.com/presentation/d/1bSkJfN-q9qhxjBrXDlR0Uup0k9E69i4v/edit?usp=sharing&ouid=105417516168271399724&rtpof=true&sd=true" class="btn btn-primary toolkit-btn" target="_blank" rel="noopener">
            Launch the Slide Deck →
          </a>
        </div>

        <!-- Facilitator Guide -->
        <div class="toolkit-card">
          <div class="toolkit-card-icon">📋</div>
          <h3 class="toolkit-card-title">Facilitator Guide</h3>
          <p class="toolkit-card-desc">
            A 3-page printable guide with the full "Say/Show/Do" script. Keep this in your hand to stay on
            track during the live session. Print it. Hold it. Own the room.
          </p>
          <a href="/Un-Tilted_GroupFacilitatorGuide.pdf" class="btn btn-outline toolkit-btn" target="_blank" download>
            Download PDF →
          </a>
        </div>

        <!-- Field Intel Lore Sheet -->
        <div class="toolkit-card">
          <div class="toolkit-card-icon">📜</div>
          <h3 class="toolkit-card-title">"Field Intel" Lore Sheet</h3>
          <p class="toolkit-card-desc">
            Hand this out to kids before they start the game. It's a physical "cheat sheet" that
            ensures they learn the skills even if they don't find every egg in Roblox.
          </p>
          <a href="#" class="btn btn-outline toolkit-btn" download>
            Print the Lore Sheet →
          </a>
        </div>

      </div>
    </div>
  </section>

  <!-- What's Inside the Deck -->
  <section class="section inside-section" id="inside">
    <div class="container">
      <div class="inside-header">
        <div>
          <span class="eyebrow">What's Inside the Deck</span>
          <h2 class="subheadline">Know what to expect<br />before you click.</h2>
        </div>
        <p class="inside-intro">
          The Master Slide Deck is a self-contained experience that guides your group
          through four stages — from first contact with the story to real-world ownership.
        </p>
      </div>

      <div class="stages-grid">

        <div class="stage-card">
          <div class="stage-header">
            <span class="stage-num">01</span>
            <span class="stage-label">Recognition</span>
          </div>
          <h3 class="stage-title">First Sticky</h3>
          <p class="stage-desc">
            Integrated expression sheets help kids identify and draw their "First Sticky" —
            their own emotional signal — anchoring the session in personal experience from
            the very start.
          </p>
          <div class="stage-tool">
            <span class="stage-tool-icon">🎨</span>
            <span>Sticky Stan Expression Sheets</span>
          </div>
        </div>

        <div class="stage-card">
          <div class="stage-header">
            <span class="stage-num">02</span>
            <span class="stage-label">Empathy</span>
          </div>
          <h3 class="stage-title">Release the Beast</h3>
          <p class="stage-desc">
            A direct link to stream the film is built right into the deck. No scrambling for
            tabs. Aaron's story opens the emotional door — the group walks through it together.
          </p>
          <div class="stage-tool">
            <span class="stage-tool-icon">🎬</span>
            <span>Embedded Film Stream Link</span>
          </div>
        </div>

        <div class="stage-card">
          <div class="stage-header">
            <span class="stage-num">03</span>
            <span class="stage-label">Application</span>
          </div>
          <h3 class="stage-title">Rage Fighters Mission</h3>
          <p class="stage-desc">
            Clear instructions and mission objectives for the Rage Fighters game are built
            into the deck. Kids know exactly what they're doing — and why — before they
            touch the controller.
          </p>
          <div class="stage-tool">
            <span class="stage-tool-icon">🎮</span>
            <span>Game Mission Objectives</span>
          </div>
        </div>

        <div class="stage-card">
          <div class="stage-header">
            <span class="stage-num">04</span>
            <span class="stage-label">Ownership</span>
          </div>
          <h3 class="stage-title">Community Mural</h3>
          <p class="stage-desc">
            Step-by-step guide for the <strong>#StickyReveal</strong> photo activity and the
            final Community Mural. Kids close the session with something they made — together.
          </p>
          <div class="stage-tool">
            <span class="stage-tool-icon">🖼️</span>
            <span>#StickyReveal + Community Mural</span>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- Technical Launchpad -->
  <section class="section launchpad-section" id="launchpad">
    <div class="container">
      <div class="launchpad-header">
        <span class="eyebrow">Technical Launchpad</span>
        <h2 class="subheadline">Check these before<br />the room fills up.</h2>
        <p class="launchpad-intro">
          Three quick tests to run while you're setting up. Five minutes now saves
          you from scrambling in front of a room full of kids.
        </p>
      </div>

      <div class="launchpad-list">

        <div class="launchpad-item">
          <div class="launchpad-item-left">
            <div class="launchpad-check">
              <span class="check-icon">📶</span>
            </div>
            <div class="launchpad-info">
              <h3 class="launchpad-title">Check the Wi-Fi</h3>
              <p class="launchpad-desc">Is Roblox whitelisted on the school or program network? Confirm access before kids arrive — this is the most common blocker.</p>
            </div>
          </div>
          <div class="launchpad-action">
            <span class="check-status">Verify before session</span>
          </div>
        </div>

        <div class="launchpad-item">
          <div class="launchpad-item-left">
            <div class="launchpad-check">
              <span class="check-icon">🔊</span>
            </div>
            <div class="launchpad-info">
              <h3 class="launchpad-title">Test the Audio</h3>
              <p class="launchpad-desc">Can everyone in the room hear the film's roar? Play a clip, walk to the back row, and confirm. The film lands on sound.</p>
            </div>
          </div>
          <div class="launchpad-action">
            <span class="check-status">Play a clip to confirm</span>
          </div>
        </div>

        <div class="launchpad-item">
          <div class="launchpad-item-left">
            <div class="launchpad-check">
              <span class="check-icon">🎮</span>
            </div>
            <div class="launchpad-info">
              <h3 class="launchpad-title">Launch Rage Fighters</h3>
              <p class="launchpad-desc">Open the Roblox server and confirm the game loads on your setup before kids are seated. Have the link ready to share or display.</p>
            </div>
          </div>
          <div class="launchpad-action">
            <a href="/rage-fighters" class="btn btn-outline">Open Game Page →</a>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="section-sm cta-section">
    <div class="container">
      <div class="cta-inner">
        <div>
          <h2 class="subheadline">Need more resources?</h2>
          <p class="body-lg">Browse the full library of engagement guides, or reach out if you want the Ryzo team in the room.</p>
        </div>
        <div class="cta-actions">
          <a href="/guides" class="btn btn-primary btn-lg">All Engagement Guides</a>
          <a href="/connect" class="btn btn-outline btn-lg">Talk to Us</a>
        </div>
      </div>
    </div>
  </section>

</Layout>

<style>
  /* Hero */
  .hub-hero {
    min-height: 85vh;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding-block: 8rem 5rem;
    position: relative;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at 20% 60%, rgba(245, 200, 66, 0.08) 0%, transparent 50%),
      radial-gradient(ellipse at 75% 30%, rgba(124, 92, 191, 0.14) 0%, transparent 50%),
      linear-gradient(to right, #080316, #1c0832);
  }

  .hero-content {
    position: relative;
    z-index: 1;
  }

  .hero-content .eyebrow { margin-bottom: 1.5rem; }

  .display {
    font-size: clamp(3rem, 7vw, 7rem);
    margin-bottom: 1.5rem;
    line-height: 1.0;
  }

  .accent-text { color: var(--accent); }

  .hero-body {
    font-size: clamp(1rem, 1.5vw, 1.2rem);
    color: var(--fg-2);
    line-height: 1.7;
    max-width: 560px;
    margin-bottom: 2rem;
  }

  /* Shared section bg */
  .toolkit-section,
  .inside-section,
  .launchpad-section,
  .cta-section {
    background: linear-gradient(to right, #080316 0%, #1c0832 100%);
    border-top: 1px solid rgba(255,255,255,0.07);
  }

  /* Toolkit */
  .toolkit-header {
    max-width: 640px;
    margin-bottom: 3.5rem;
  }

  .toolkit-header .eyebrow { margin-bottom: 1rem; }
  .toolkit-header .subheadline { margin-bottom: 1rem; }

  .toolkit-intro {
    font-size: var(--text-sm);
    color: var(--fg-2);
    line-height: 1.7;
  }

  .toolkit-grid {
    display: grid;
    grid-template-columns: 1.4fr 1fr 1fr;
    gap: 1.25rem;
    align-items: stretch;
  }

  .toolkit-card {
    position: relative;
    background: var(--bg-2);
    border: 1px solid var(--border);
    border-radius: var(--radius-xl);
    padding: 2.5rem 2rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    transition: var(--transition);
  }

  .toolkit-card:hover {
    border-color: var(--border-hover);
    transform: translateY(-3px);
  }

  .toolkit-card--primary {
    border-color: rgba(245, 200, 66, 0.25);
    border-top: 2px solid var(--accent);
    background: linear-gradient(135deg, var(--bg-2) 0%, rgba(245, 200, 66, 0.04) 100%);
  }

  .toolkit-card--primary:hover {
    border-color: rgba(245, 200, 66, 0.4);
  }

  .toolkit-card-icon {
    font-size: 2rem;
  }

  .toolkit-card-badge {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    font-size: var(--text-xs);
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--accent);
    background: var(--accent-dim);
    border: 1px solid rgba(245, 200, 66, 0.3);
    border-radius: 100px;
    padding: 0.2em 0.75em;
  }

  .toolkit-card-title {
    font-family: var(--font-display);
    font-size: var(--text-2xl);
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.1;
  }

  .toolkit-card-desc {
    font-size: var(--text-sm);
    color: var(--fg-2);
    line-height: 1.7;
    flex: 1;
  }

  .toolkit-card-desc strong {
    color: var(--fg);
    font-weight: 600;
  }

  .toolkit-btn {
    margin-top: auto;
    align-self: flex-start;
  }

  /* Inside the Deck */
  .inside-header {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: end;
    margin-bottom: 3rem;
  }

  .inside-header .eyebrow { margin-bottom: 0.75rem; }

  .inside-intro {
    font-size: var(--text-sm);
    color: var(--fg-2);
    line-height: 1.75;
    align-self: end;
  }

  .stages-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1px;
    background: var(--border);
    border: 1px solid var(--border);
    border-radius: var(--radius-xl);
    overflow: hidden;
  }

  .stage-card {
    background: var(--bg-2);
    padding: 2rem 1.75rem;
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    transition: var(--transition);
  }

  .stage-card:hover {
    background: var(--bg-3);
  }

  .stage-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.25rem;
  }

  .stage-num {
    font-family: var(--font-display);
    font-size: var(--text-xs);
    font-weight: 800;
    color: var(--accent);
    letter-spacing: 0.08em;
  }

  .stage-label {
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--fg-3);
  }

  .stage-title {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 800;
    letter-spacing: -0.02em;
    line-height: 1.2;
  }

  .stage-desc {
    font-size: var(--text-sm);
    color: var(--fg-2);
    line-height: 1.65;
    flex: 1;
  }

  .stage-desc strong {
    color: var(--fg);
    font-weight: 600;
  }

  .stage-tool {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    padding-top: 0.75rem;
    border-top: 1px solid var(--border);
    font-size: var(--text-xs);
    color: var(--fg-3);
    font-weight: 500;
    margin-top: auto;
  }

  .stage-tool-icon { font-size: 1rem; }

  /* Launchpad */
  .launchpad-header {
    max-width: 620px;
    margin-bottom: 3rem;
  }

  .launchpad-header .eyebrow { margin-bottom: 1rem; }
  .launchpad-header .subheadline { margin-bottom: 1rem; }

  .launchpad-intro {
    font-size: var(--text-sm);
    color: var(--fg-2);
    line-height: 1.7;
  }

  .launchpad-list {
    display: flex;
    flex-direction: column;
    border-top: 1px solid rgba(255,255,255,0.07);
  }

  .launchpad-item {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 3rem;
    padding: 2rem 0;
    border-bottom: 1px solid rgba(255,255,255,0.07);
  }

  .launchpad-item:last-child { border-bottom: none; }

  .launchpad-item-left {
    display: flex;
    align-items: center;
    gap: 1.75rem;
    flex: 1;
  }

  .launchpad-check {
    width: 56px;
    height: 56px;
    background: var(--bg-3);
    border: 1px solid var(--border);
    border-radius: var(--radius-lg);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .check-icon { font-size: 1.5rem; }

  .launchpad-info {
    display: flex;
    flex-direction: column;
    gap: 0.375rem;
  }

  .launchpad-title {
    font-family: var(--font-display);
    font-size: var(--text-xl);
    font-weight: 800;
    letter-spacing: -0.02em;
  }

  .launchpad-desc {
    font-size: var(--text-sm);
    color: var(--fg-2);
    line-height: 1.6;
    max-width: 520px;
  }

  .launchpad-action { flex-shrink: 0; }

  .check-status {
    font-size: var(--text-xs);
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--fg-3);
    background: var(--bg-3);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 0.4em 0.875em;
    white-space: nowrap;
  }

  /* CTA */
  .cta-inner {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 3rem;
    flex-wrap: wrap;
  }

  .cta-inner > div:first-child {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    max-width: 480px;
  }

  .cta-actions { display: flex; gap: 0.875rem; flex-wrap: wrap; flex-shrink: 0; }

  @media (max-width: 1024px) {
    .toolkit-grid { grid-template-columns: 1fr 1fr; }
    .toolkit-card--primary { grid-column: span 2; }
    .stages-grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 768px) {
    .toolkit-grid { grid-template-columns: 1fr; }
    .toolkit-card--primary { grid-column: span 1; }
    .inside-header { grid-template-columns: 1fr; gap: 1.5rem; }
    .stages-grid { grid-template-columns: 1fr; }
    .launchpad-item { flex-direction: column; align-items: flex-start; gap: 1.25rem; }
    .launchpad-item-left { flex-direction: column; align-items: flex-start; gap: 1rem; }
    .cta-inner { flex-direction: column; align-items: flex-start; }
  }
</style>
